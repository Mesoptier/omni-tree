{
  "name": "omni-tree",
  "description": "Tool for creating data to enter into a mocked file systems.",
  "version": "0.0.1",
  "scripts": {
    "test": "./node_modules/.bin/mocha test --compilers js:babel-register",
    "compile": "./node_modules/.bin/babel src -d lib",
    "prepublish": "npm run compile"
  },
  "main": "./lib/tree.js",
  "devDependencies": {
    "babel-cli": "^6.3.15",
    "babel-eslint": "^4.1.6",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "eslint": "^1.10.3"
  }
}
